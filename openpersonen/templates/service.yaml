apiVersion: v1
kind: Service
metadata:
  name: {{ include "openpersonen.fullname" . }}
  labels:
    {{- include "openpersonen.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "openpersonen.selectorLabels" . | nindent 4 }}
